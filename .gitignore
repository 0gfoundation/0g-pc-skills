# internal specs, source docs, and local git-history archive — never pushed
spec/

# local tooling artifacts
.DS_Store
*.skill
.claude/

# local probe runs — real-machine verification records, never pushed
0g-probe-claude/
0g-probe-codex/
